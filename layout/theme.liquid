<!DOCTYPE html>
<html lang="{{ shop.locale.iso_code }}">
<head>
  <meta charset="utf-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width,initial-scale=1.0">
  <title>{{ page_title }}</title>
  {{ content_for_header }}
</head>
<body>

  {% comment %} ✅ HEADER — MUST MATCH FILE NAME EXACTLY {% endcomment %}
  {% section 'custom-header' %}

  <main role="main">
    {{ content_for_layout }}
  </main>

  {% comment %} ✅ FOOTER — MUST MATCH FILE NAME EXACTLY {% endcomment %}
  {% section 'custom-footer' %}

  {{ content_for_footer }}
</body>
</html>
