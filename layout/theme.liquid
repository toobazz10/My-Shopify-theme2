<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{ page_title }}</title>
  {{ content_for_header }}
</head>
<body>

  {% comment %} ⚠️ NAME MUST MATCH FILES EXACTLY! {% endcomment %}
  {% section 'custom-header' %}

  <main>
    {{ content_for_layout }}
  </main>

  {% comment %} ⚠️ NAME MUST MATCH FILES EXACTLY! {% endcomment %}
  {% section 'custom-footer' %}

  {{ content_for_footer }}
</body>
</html>
